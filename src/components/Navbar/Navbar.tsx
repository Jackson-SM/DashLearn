import React from 'react';
import { Logo } from '../Logo/Logo';
import { StyledNavbar } from './styles';

export const Navbar = () => {
  return (
    <StyledNavbar>
      <Logo />
    </StyledNavbar>
  );
};
